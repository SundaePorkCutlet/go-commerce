apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-user
  namespace: go-commerce
  labels:
    app: postgres-user
    app.kubernetes.io/name: postgres-user
    app.kubernetes.io/part-of: go-commerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-user
  template:
    metadata:
      labels:
        app: postgres-user
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: user
            - name: POSTGRES_USER
              value: admin
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: orderfc-secret
                  key: db-password
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "admin", "-d", "user"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: postgres-user-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-user-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-user
  namespace: go-commerce
  labels:
    app: postgres-user
    app.kubernetes.io/name: postgres-user
    app.kubernetes.io/part-of: go-commerce
spec:
  selector:
    app: postgres-user
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
